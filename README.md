# mlops-deployment-project
This project demonstrates a complete end-to-end MLOps CI/CD pipeline. It uses the Iris Dataset and Random Forest Classifier for model training. The deployment is automated using a Jenkins Multi-branch Pipeline that handles different environments based on Git branches and tags
